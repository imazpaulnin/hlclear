# Capturas

Las capturas deben generarse contra la PWA publicada o contra un servidor local accesible por un navegador del host.

En esta sesión, la PWA quedó servida localmente y compilada, pero la captura automática desde el navegador del host quedó bloqueada por restricciones del propio entorno Windows y del navegador embebido de Codex, no por la aplicación.
